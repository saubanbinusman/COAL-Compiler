LET x AS INTEGER

IF (10 < 9)
    PRINTLINE "A"
ELSE
    IF (10 < 8)
        PRINTLINE "B"
    ELSE
        IF (10 < 7)
            PRINTLINE "C"
        ELSE
            PRINTLINE "D"
        ENDIF
    ENDIF
ENDIF